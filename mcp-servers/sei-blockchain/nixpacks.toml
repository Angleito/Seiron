# Nixpacks configuration for SEI Blockchain MCP
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd .. && npm ci"]

[phases.build]
cmds = [
    "cd .. && npm install -g typescript",
    "cd .. && tsc"
]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"