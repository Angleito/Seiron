# Nixpacks configuration for Hive Intelligence MCP
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
    "npm install -g typescript",
    "tsc"
]

[start]
cmd = "node dist/hive-intelligence/server-v2.js"

[variables]
NODE_ENV = "production"